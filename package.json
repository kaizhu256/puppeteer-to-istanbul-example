{
  "name": "puppeteer-to-istanbul-example",
  "version": "1.0.0",
  "description": "example of using puppeteer-to-istanbul to output istanbul reports from puppeteer coverage",
  "main": "index.js",
  "scripts": {
    "precoverage0": "rimraf coverage && rimraf .nyc_output",
    "coverage": "node index.js",
    "postcoverage0": "nyc report --reporter=html && open coverage/index.html",
    "test": "standard"
  },
  "keywords": [
    "puppeteer",
    "istanbul",
    "reports",
    "coverage"
  ],
  "author": "Ben Coe <ben@npmjs.com>",
  "license": "ISC",
  "dependencies": {
    "nyc": "^14.1.1",
    "puppeteer": "^1.1.1",
    "puppeteer-to-istanbul": "^1.2.2",
    "rimraf": "^2.6.2"
  },
  "devDependencies": {
    "standard": "^11.0.0"
  }
}
